# Release notes

Per-version, newest first. The release ritual in `docs/LINEAR_AND_GITHUB.md` writes here: one
section per version, which stops saying `(unreleased)` when the tag exists. Only the user
declares a release.

## Unreleased

### Added

- The documentation set, the dev flow, the board, the roadmap (VR-230).

### Changed

Nothing yet.

### Fixed

Nothing yet.

### Upgrading

Nothing to upgrade from.
